<nav class="bg-gray-800">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
            <div class="flex items-center">
                <div class="flex-shrink-0 text-white text-xl">
                    Admin
                </div>
                <div class="hidden md:block">
                    <div class="ml-10 flex items-baseline space-x-4">
                        <div class="flex">
                            <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
                            <a href="{{ path('admin') }}" class="bg-gray-900 text-white px-3 py-2 rounded-md text-sm font-medium">
                                Dashboard
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            <div class="flex rounded bg-gray-700">
                <div class="text-white p-2 text-sm border-r border-white">Hello, {{ app.user.name }}!</div>
                <a href="{{ path('app_logout') }}" class="text-white px-3 py-2 rounded-r text-sm font-medium hover:bg-gray-500 hover:text-blue-50">Log out</a>
            </div>
        </div>
    </div>
</nav>